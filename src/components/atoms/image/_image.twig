<img
  src="{{ src }}"
  {{ class ? 'class="' ~ class ~ '"' }}
  {{ alt ? 'alt="' ~ alt ~ '"' }}
  {{ width ? 'width="' ~ width ~ '"' }}
  {{ height ? 'height="' ~ height ~ '"' }}
  {{ attributes }}
>
